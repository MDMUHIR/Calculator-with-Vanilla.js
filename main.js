import "./style.css";
// import { setupCounter } from "./counter.js";

// setupCounter(document.querySelector("#counter"));
 import { setupCalculator } from "./calcutator";
 setupCalculator(document.querySelector(".container"))